# JavaScript Core Maintenance — Session 3

Practiced JavaScript arrays and loops through QA-style exercises:

- creating arrays
- accessing elements by index
- using `.length`
- getting the first and last element
- changing array elements
- adding elements with `.push()`
- removing elements with `.pop()`
- storing values returned by `.pop()`
- checking whether an array is empty
- using arrays as function parameters
- iterating through arrays with a `for` loop
- numbering test results with `i + 1`
- using `if/else` inside loops
- counting passed tests and error status codes

The final exercises focused on validating test queues, processing status codes, and counting test results.
