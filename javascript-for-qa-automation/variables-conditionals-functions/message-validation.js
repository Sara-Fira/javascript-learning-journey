const expectedMessage = 'Welcome, standard_user!';
const actualMessage = 'Welcome, standard_user!';

if (actualMessage === expectedMessage) {
  console.log('Message validation passed');
} else {
  console.log('Message validation failed');
}
